/**
 * 补充 getTabData 函数，
 * 从参数 products 中获取所有 goodsType（值如：["1","2"]）字段包含参数 gType（值如：“2”）的商品数据，并存入数组 tabProducts 中返回
 * 若没有匹配数据，则返回空数组
 */
const getTabData = (products, gType) => {
  let tabProducts = []; // 用于返回的对应选项的数据数组
  // TODO：在此处补充代码实现需求

  return tabProducts;
};

module.exports = getTabData;
